<UserControl x:Class="CashDispenserGUI.BalanceControl"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="Balance:" Style="{StaticResource TextBlockStyle}"/>
        <TextBlock Grid.Column="1" Text="{Binding CashDispenser.Balance}" Style="{StaticResource TextBlockStyle}"/>
    </Grid>
</UserControl>
